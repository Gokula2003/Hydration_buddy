# Docker
*.log
.env.docker

# Docker Compose - Keep for local development only
# Uncomment the next line if deploying to Render to prevent conflicts
# docker-compose.yml

# Keep the main docker files
!Dockerfile
!docker-entrypoint.sh
!nginx.conf
!supervisord.conf
!.dockerignore
